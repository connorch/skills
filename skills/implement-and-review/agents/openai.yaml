interface:
  display_name: "Implement and Review"
  short_description: "Take an approved plan through implementation, QA, and PR review"
  default_prompt: "Use $implement-and-review to implement the approved plan, QA it, file the PR, work the review loop, and report back."
policy:
  allow_implicit_invocation: true
