# QA/test artifacts from running the skills (screenshots, reports, etc.)
output/

# Personal, untracked skills - keep the directory, ignore its contents
skills-local/*
!skills-local/.gitkeep

# workspace
node_modules
.DS_Store
*.log
*.tsbuildinfo
apps/*/dist
packages/*/dist
.vite-plus

# wovn-files worker dev artifacts (wrangler)
.wrangler
.dev.vars
worker-configuration.d.ts
