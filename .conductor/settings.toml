"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run_mode = "nonconcurrent"

[scripts.run.ship-fleet]
available_in = ["local"]
command = "pnpm ship:fleet"
default = true
icon = "package"
