{
  "name": "skills",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "vp config --no-agent",
    "conductor:run": "node scripts/conductor-run.mjs",
    "dev:wovn-files": "vp run --filter wovn-files dev",
    "build": "vp run -r build",
    "typecheck": "vp run -r typecheck",
    "lint": "vp lint --report-unused-disable-directives",
    "fmt": "vp fmt",
    "fmt:check": "vp fmt --check",
    "test": "node --test",
    "wovn:install": "node scripts/install-wovn.mjs",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/dist packages/*/dist .vite-plus apps/*/.vite-plus packages/*/.vite-plus"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": "^24.13.1"
  },
  "packageManager": "pnpm@11.10.0"
}
